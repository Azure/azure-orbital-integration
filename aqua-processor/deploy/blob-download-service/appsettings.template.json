{
  "applicationInsights": {
    "connectionString": "${APP_INSIGHTS_CONNECTION_STRING}"
  },
  "blobDownloadOptions": {
    "userAssignedManagedIdentityName": "",
    "environmentName": "${ENVIRONMENT_NAME}",
    "ServiceBusReceivers": [
      {
        "receiverName": "${RECEIVER_NAME}",
        "serviceBusConnectionString": "${SERVICE_BUS_CONNECTION_STRING}",
        "serviceBusQueueName": "${SERVICE_BUS_QUEUE_NAME}",
        "blobConnectionString": "${BLOB_CONNECTION_STRING}",
        "localBlobDownloadPath": "${LOCAL_BLOB_DOWNLOAD_PATH}",
        "allowedEventTypes": [
          "${ALLOWED_EVENT_TYPE}"
        ]
      }
    ]
  },
  "Logging": {
    "ApplicationInsights": {
      "LogLevel": {
        "Default": "Information",
        "Microsoft": "Warning",
        "System": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    },
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "System": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}